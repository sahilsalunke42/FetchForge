{
  "name": "fetchforge",
  "version": "1.0.0",
  "description": "A TypeScriot-first, unified HTTP client that adds retries, timeoutss, rate-limits, caching, queueing and middleware on top of fetch and exposes a clean, typed API for backend and browser.",
  "keywords": [
    "sdk",
    "HTTP-client",
    "fetch"
  ],
  "repository": {
    "type": "git",
    "url": "FetchForge"
  },
  "license": "ISC",
  "author": "Sahil Salunke",
  "type": "commonjs",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "nodemon": "^3.1.11",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
